{
  "name": "three-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "copy": "cp cara.png dist/ && cp moon.jpg dist/ && cp normal.jpg dist/ && cp space.jpg dist/",
    "build": "vite build && npm run copy",
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.3.0"
  },
  "dependencies": {
    "three": "^0.128.0"
  }
}
